#include "commandHandler.hpp"

std::string CommandHandler::runCommand(std::string& command)
{
    switch (command)
    {
    case(VIEW_USER_INFORMATION)
        break;
    
    default:
        break;
    }
}